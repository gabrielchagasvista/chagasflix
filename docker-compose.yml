version: '3'

services:
  apache:
    image: httpd:latest
    container_name: estag-flix
    ports:
      - 81:80
    volumes:
    - type: bind
      source: ./estag-flix
      target: /usr/local/apache2/htdocs/estag-flix