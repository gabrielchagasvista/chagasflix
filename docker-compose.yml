version: '3'

services:
  apache:
    image: httpd:latest
    container_name: estagflix
    ports:
      - 81:80
    volumes:
      - .:/usr/local/apache2/htdocs/estagflix
    #- type: bind
    #  source: .
    #  target: /usr/local/apache2/htdocs/estagflix